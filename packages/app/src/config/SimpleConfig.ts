
export class SimpleConfig {
  brand: string;
  theme: string;
  view: string;

  constructor(brand: string, theme: string, view: string) {
    this.brand = brand;
    this.theme = theme;
    this.view = view;
  }
}